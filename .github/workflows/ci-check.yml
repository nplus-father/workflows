name: CI Check

# Placeholder CI check to satisfy branch-protection "require status check" rules.
# Replace with real linting/testing later if needed.

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge
        run: echo "Placeholder CI check passed to satisfy Branch Protection."
