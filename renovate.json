{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "timezone": "Asia/Taipei",
  "schedule": ["* 0 1 */3 *"],
  "dependencyDashboard": true,
  "automerge": true,
  "automergeType": "pr",
  "platformAutomerge": true,
  "separateMajorMinor": false,
  "separateMultipleMajor": false,
  "rebaseWhen": "conflicted",
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "packageRules": [
    {
      "description": "Major updates only: group into one PR, run CI Check, automerge on green",
      "matchUpdateTypes": ["major"],
      "groupName": "major dependencies",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "Skip typescript major (TS7 native compiler restructures deps and breaks npm lockfile sync); upgrade manually if ever needed",
      "matchPackageNames": ["typescript"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "book convention plugin from GitHub Packages",
      "matchPackageNames": ["com.andrewnplus.book"],
      "registryUrls": ["https://maven.pkg.github.com/nplus-father/book-gradle-conventions"]
    },
    {
      "description": "Skip patch/minor/digest/pin entirely: no PR, no CI (major-only policy, saves Actions minutes). Placed last so it overrides package-specific re-enables above.",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "enabled": false
    },
    {
      "description": "Hugo is exempt from the major-only policy: it never leaves 0.x, so EVERY release is a minor and the rule above would silence it forever. Placed after that rule so this re-enable wins. Safe to automerge because the version now lives in exactly one place — the hugo-version default below. The runner image deliberately does not bake Hugo (see gh-runner/Dockerfile.runner); actions-hugo installs it per job, so there is nothing to keep in sync and the 'Verify Hugo version' guard cannot drift.",
      "matchPackageNames": ["gohugoio/hugo"],
      "matchUpdateTypes": ["minor", "patch"],
      "enabled": true,
      "automerge": true,
      "groupName": "hugo"
    },
    {
      "description": "nplus-book-core is exempt from the major-only policy, for the same structural reason as Hugo: its v2.x tags are unusable (the repo's go.mod module path lacks the /v2 suffix, so Go refuses to resolve them), which means every theme release has to ship as a v1.x minor. Under the major-only rule those were silenced outright — the theme could not reach a single book repo, and the handbooks sat on v1.5.0 while v1.6.0 had already shipped. Placed after that rule so this re-enable wins. Cost is understood and accepted: each release fans out to ~1400 repos, one CI build and Pages deploy apiece.",
      "matchPackageNames": ["github.com/nplus-father/nplus-book-core"],
      "matchUpdateTypes": ["minor", "patch"],
      "enabled": true,
      "automerge": true,
      "groupName": "book core"
    }
  ],
  "hostRules": [
    {
      "description": "Auth for book-gradle-conventions on GitHub Packages (requires PACKAGES_TOKEN secret with read:packages in Mend)",
      "matchHost": "maven.pkg.github.com",
      "hostType": "maven",
      "username": "nplus-father",
      "password": "{{ secrets.PACKAGES_TOKEN }}"
    }
  ],
  "gomod": {
    "enabled": true,
    "postUpdateOptions": ["gomodTidy"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Hugo version in GitHub Actions workflows",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": ["hugo-version:\\s*['\"]?(?<currentValue>[0-9.]+)['\"]?"],
      "depNameTemplate": "gohugoio/hugo",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v(?<version>.+)$"
    }
  ]
}
