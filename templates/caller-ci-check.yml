# Drop this file into a HugoBook repo as .github/workflows/ci-check.yml.

name: CI Check

on:
  pull_request:
    branches: [main]

jobs:
  check:
    uses: nplus-father/workflows/.github/workflows/ci-check.yml@v1
